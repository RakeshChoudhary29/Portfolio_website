# Web3Forms access key for the contact form (free at https://web3forms.com —
# enter your email, the key arrives instantly). This key is public by design;
# it only identifies which inbox receives submissions, so it is safe to expose
# in the client bundle. Without it, the Contact section falls back to direct
# email/LinkedIn buttons and no form is rendered.
VITE_WEB3FORMS_KEY=
